# GLM-5.2 网关接入 DeepSeek Harness：实测结论与适配方案

## 1. API 实测结果（your-gateway-host:port，OpenAI 兼容 `/v1/chat/completions`）

| 测试 | 结果 |
|---|---|
| 非流式对话 | ✅ 标准 OpenAI `chat.completion` 格式；assistant message 带 `reasoning_content`（思考）字段 |
| 流式 SSE（`stream:true`） | ✅ `role` → `reasoning_content` 增量 → `content` 增量 → 带 `finish_reason` 的空 content 分片 → 尾随 usage 分片 → `[DONE]` |
| `stream_options.include_usage` | ✅ 支持，usage 作为独立尾随分片返回 |
| `thinking:{type}` + `reasoning_effort` 参数 | ✅ 接受（会开启 thinking 增量输出） |
| system 角色 / `max_tokens` | ✅ 接受 |
| 工具定义 `tools` | ✅ 正常触发 `finish_reason:"tool_calls"` |

### 两个“工具调用不兼容”的关键 bug（就是你的 API 不能直接用 DSH 的原因）

**Bug 1 — 非流式响应的 `arguments` 带多余 `{}` 前缀（稳定复现）**
```json
"tool_calls":[{"id":"call_xxx","type":"function",
  "function":{"name":"get_weather","arguments":"{}{\"city\":\"北京\"}"}}]
```
`arguments` 应为 `{"city":"北京"}`，实际多了前导 `{}`。DSH harness 解析工具参数时
`JSON.parse` 失败会**静默降级为原始字符串**（见 dsh-agent-loop `parseArguments`），
导致工具收到字符串而非对象 → 工具校验失败/调用异常。

**Bug 2 — 流式工具调用的后续分片重复发送 `function.name:""`（稳定复现）**
```
chunk1: {"delta":{"tool_calls":[{"index":0,"id":"call_x","type":"function",
         "function":{"name":"get_weather","arguments":""}}]}}
chunk2+: {"delta":{"tool_calls":[{"index":0,"function":{"name":"","arguments":"{\""}}]}}
```
DSH 的翻译层（dsh-llm-deepseek `translate()`）对每个分片执行
`if (name !== undefined) block.name = name`，因此空串 `""` 会把第一分片写入的
`get_weather` **覆盖成空名** → 工具按空名查找失败。

（流式路径的 arguments 增量拼接后是干净 JSON，Bug 1 只出现在非流式；但 harness 只走流式，
Bug 2 是真正会在 harness 里爆掉的点。）

### 多轮回传兼容性（实测矩阵）
- 回传 assistant 的 `tool_calls`（含 `content:""`）✅ 接受，且**干净参数**时工具结果会被模型使用
- 回传 assistant 的 `reasoning_content` 到历史 ✅ 接受
- 回传**带 `{}` 脏前缀**的 arguments：网关不报错但**静默丢弃该工具结果**（模型像没调用过工具一样作答）
- assistant 工具消息带脏参数 + `content:""` 组合会触发网关后端（Anthropic 形状校验）500

> 注：网关后端校验报错里的 `ClaudeContentBlock*` 类型说明它内部是 Anthropic 风格协议网关，
> 但对外暴露的 OpenAI 兼容接口我们验证的字段全部可用。

## 2. DSH 侧机制结论

- 模型走 `dsh-llm` 的 **provider route** seam；每个 provider 是一个 cordis 插件
  （`{name, inject:["llm"], apply}`），通过 `ctx.llm.registerConfigurableProviders(...)` +
  `registerAdapter([provider], adapter)` 注册，settings namespace 配置（baseURL、models、
  apiKeyEnv、thinking 等），密钥走 `ctx.credentials` 或受信环境变量。
- 现有 `dsh-llm-deepseek` 正是“OpenAI 兼容 chat-completions + SSE + reasoning_content”适配器，
  与这台网关**协议形态几乎完全兼容**（上面表格已逐一验证其请求字段），只差两个工具调用 bug。
- 因此最小可靠方案：**fork dsh-llm-deepseek 做一个网关专用 provider 插件**，而不是从零写。
  已按此落地 → `dsh-llm-glm-gateway/`。

## 3. 已落地的适配插件 `dsh-llm-glm-gateway/`

基于 `@deepseek-ai/dsh-llm-deepseek@0.1.1-rc.2`（MIT）编译产物 fork，改动点：

1. **工具名防空覆盖**（`translate()`）：`name`/`id` 只在“字符串且非空”时写入，
   忽略网关重复的空 `name:""` 分片 → 工具名不再被清空。
2. **arguments 清洗**（新增 `sanitizeToolArguments()`，在 `closeBlock()` 应用）：
   循环剥掉前导的空 `{}` 前缀（支持 `{}{...}`、`{}{}{...}`），保留其余原文；
   对正常单对象（含嵌套对象）零影响。→ 无论流式/非流式进入 harness 的参数都是干净 JSON。
3. 身份替换：provider route `glm-gateway`（UI 显示 “GLM Gateway”）、插件名
   `llm-glm-gateway`、settings namespace `llm-glm-gateway`。
4. 默认配置：模型目录仅 `glm-5.2`、baseURL 需在配置中显式给出（如
   `http://your-gateway-host:port/v1`）、
   `apiKeyEnv` 默认 `TEST_KEY`、环境变量覆盖名 `GLM_GATEWAY_BASE_URL`。
5. `node --check` 通过；`package.json` 的 peer/deps 与源码包一致。

其余逻辑（serialize、SSE 解析、reasoning 回传、错误分类、usage 换算、重试策略、thinking/
reasoningEffort 映射）与官方 DeepSeek 适配器相同——这些形态已在上表逐项实测兼容。

## 4. 接入步骤（待执行，需要你确认）

1. 把插件装进 web profile：
   ```powershell
   dsh plugin --profile web add C:\Users\admin\Desktop\dsh-plugin-fix-tool-calls\dsh-llm-glm-gateway
   ```
2. 在 `%USERPROFILE%\.dsh\profiles\web\package.json` 的 `dsh.profile.bundles` 里追加
   `"dsh-llm-glm-gateway"`（如需要，也可改用 cordis.patch.yml 插入 entry，见下）。
3. 若自动发现不生效，在 `%USERPROFILE%\.dsh\profiles\web\cordis.patch.yml` 追加 entry：
   ```yaml
   - id: llm-glm-gateway
     name: dsh-llm-glm-gateway
     config:
       apiKeyEnv: TEST_KEY
       baseURL: http://your-gateway-host:port/v1
       thinking: enabled
       reasoningEffort: high
       maxTokens: 131072     # 网关校验 max_tokens ∈ [1,131072]，勿设更大
       models:
         - id: glm-5.2
           name: GLM-5.2
   ```
4. 密钥：确保启动 dsh 的 shell 里有 `TEST_KEY` 环境变量（你已写入 Windows 用户级，
   新开的终端会继承）；或重启后在 Web “Models” 设置页为 GLM Gateway 路由填写。
5. 重启 `dsh web`，在模型选择器/设置页应出现 “GLM Gateway” 与 `glm-5.2`，然后实测一轮带工具调用
   （如天气查询）验证。

## 5. 验证命令速查（故障排查用）

```powershell
# 看组合配置树里 provider 是否就位
dsh --profile web --dump-config | Select-String -Pattern 'glm-gateway'
# 直接请求（非流式）看 arguments 是否仍带 {} 前缀
curl http://your-gateway-host:port/v1/chat/completions -H "Authorization: Bearer $env:TEST_KEY" ...
```
