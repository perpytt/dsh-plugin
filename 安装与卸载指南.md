# GLM Gateway 插件：安装 / 验证 / 卸载 全流程指南

> 适用：Windows + 本机 dsh（`%USERPROFILE%\.dsh\profiles\web`）
> 插件目录：`C:\Users\admin\Desktop\dsh-plugin-fix-tool-calls\dsh-llm-glm-gateway`
> ⚠️ 第 3 步重启会中断当前 web GUI（包括正在进行的会话），建议先看完本文件再动手。

---

## 一、安装

### 第 1 步：把插件包装进 web profile（只装依赖，还没激活）

```powershell
# 在任意目录执行（dsh 是全局 CLI；找不到就先打开“开始菜单→PowerShell”确保 npm 全局目录在 PATH）
dsh plugin --profile web add "C:\Users\admin\Desktop\dsh-plugin-fix-tool-calls\dsh-llm-glm-gateway"
```

- 作用：改写 `%USERPROFILE%\.dsh\profiles\web\package.json`（加 dependencies）+ 把包放进 profile 的 node_modules。
- 可选自检：
  ```powershell
  Test-Path "$env:USERPROFILE\.dsh\profiles\web\node_modules\dsh-llm-glm-gateway"
  # 若上面为 False，也可查 profiles\node_modules 下同名目录
  Get-ChildItem "$env:USERPROFILE\.dsh\profiles\node_modules" -Filter 'dsh-llm-glm-gateway'
  ```

### 第 2 步：激活（往运行树里插入 provider 行）

编辑 `%USERPROFILE%\.dsh\profiles\web\cordis.patch.yml`，把原内容（一个 `[]`）**整体替换**为：

```yaml
# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
- insert:
    - id: llm-glm-gateway
      name: dsh-llm-glm-gateway
      config:
        apiKeyEnv: TEST_KEY
        baseURL: http://your-gateway-host:port/v1
        maxTokens: 131072
        models:
          - id: glm-5.2
            name: GLM-5.2
```

> `config` 与代码默认值一致，纯属“显式钉住”，删掉整段 config 也能用默认。

### 第 3 步：重启 web

1. **先停掉旧的 dsh web**：在当初启动它的终端按 Ctrl+C；若找不到，任务管理器结束对应 node 进程（小心别杀错——不确定就问）。
2. 再启动：
   ```powershell
   dsh web
   ```

---

## 二、安装后验证

1. **模型选择器**：新对话里点模型名，应出现 **GLM Gateway**（provider）→ 模型 `glm-5.2`；并能看到 reasoning effort 档位（Off/Low/High/Max，默认 High）。
2. **跑一轮工具调用测试**（关键！）：
   - 提示词示例：“帮我查一下北京的天气”（若 harness 里有可用工具）；
   - 预期：模型先思考 → 触发工具调用（轨迹里工具名完整如 `get_weather`、参数是干净 JSON `{"city":"北京"}`）→ 工具返回 → 模型给出最终正文回答。
   - 若正文又“消失”，把该轮的 usage 截图/记录发我，重点看 `completion_tokens` 是否打满。
3. 其他 API（DeepSeek / 之后配的 Moonshot）应完全不受影响。

---

## 三、卸载（完全还原，不留痕迹）

### 第 1 步：移除包依赖
```powershell
dsh plugin --profile web remove dsh-llm-glm-gateway
```

### 第 2 步：还原激活条目
把 `cordis.patch.yml` 改回：
```yaml
# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
[]
```

### 第 3 步（可选）：清掉页面存的配置/密钥
- 若在 Models 页给 GLM Gateway 存过 key/改过默认 → 页面里删除；或删 `%USERPROFILE%\.dsh\settings.yaml` 中 `llm-glm-gateway:` 分节。
- 环境变量 `TEST_KEY` 是你自己的，**不要动**。

### 第 4 步：重启
```powershell
dsh web
```
> ⚠️ 第 1、2 步都做完再重启；只做其一会导致加载失败。

**确认卸干净**：模型选择器无 GLM Gateway；`dsh --profile web --dump-config` 无 `llm-glm-gateway`。

---

## 四、（可选）同场加装 Moonshot / Kimi 官方 API —— 纯配置，走 pi-ai

如果你要用官方 Moonshot API，**不用写插件**。在上面第 2 步的 `cordis.patch.yml` 末尾追加（与 GLM 行共存）：

```yaml
- id: llm-pi-ai
  config:
    providers:
      moonshotai-cn:              # 国内站；国际站用 moonshotai
        apiKeyEnv: MOONSHOT_API_KEY
```

密钥两种给法（二选一）：
1. 在启动 dsh 的终端里先 `setx MOONSHOT_API_KEY "你的key"`（新开终端生效）后重启 dsh web；
2. 或重启后在 Models 页给 Moonshot 路由填 key（写入 `settings.yaml`，热生效）。

pi-ai 已内置 Moonshot 的模型目录与协议适配（max_tokens 字段、不支持 reasoning_effort、Kimi 工具消息怪癖等），通常无需额外配置。若个别模型要调上下文/输出上限，可在该路由下加：
```yaml
        models:
          - id: kimi-k2-xxxx
            contextWindow: 200000
            maxTokens: 32768
```

卸载 Moonshot：删掉上面 `- id: llm-pi-ai` 那段 + 页面/settings 里对应 key，重启即可。

---

## 五、常见问题

| 现象 | 原因/处理 |
|---|---|
| 重启后没有 GLM Gateway | 第 1 步没装成功或第 2 步 YAML 写错（缩进必须两个空格层级）→ 检查后重装 |
| 报 `MISSING_CREDENTIAL` | dsh 进程环境里没有 `TEST_KEY`（老终端不继承新加的 User 变量）→ 新开终端重启 dsh，或 Models 页存 key |
| 报 `[1210] max_tokens 参数非法` | 某处把 maxTokens 配成了 >131072 → 改回 131072 |
| 正文又“消失” | 记下该轮 usage 发我；先别急着卸，多半是某轮 max_tokens 被调小 |
| 改了插件代码重启不生效 | pnpm 本地依赖若为拷贝非链接 → 重跑一次第 1 步 add 再重启 |
